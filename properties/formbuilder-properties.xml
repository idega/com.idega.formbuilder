<?xml version="1.0" encoding="UTF-8"?>

<!-- PLEASE COMMENT YOUR ENTRIES -->
<config>

	<!-- autofill components are any component type bind to jsf value binding expression -->    
    <properties name="autofill-components">
    	<!-- text component autofilled with user's name -->
        <property name="fbc_text" value="loginSession.user.name" />
        <property name="fbc_text" value="loginSession.user.name" />
        <property name="fbc_text" value="loginSession.user.name" />
    </properties>
    
    <!-- external data sources map with direct links to xml files containing data for select components -->
    <properties name="select-external_data_sources">
    	<!-- cities, stored in the local webdav repository
        <property name="webdav_cities" value="webdav:/files/forms/data_sources/webdav_cities.xml" />
        -->
        <!-- post numbers
        <property name="postnumer" value="http://testegov.idega.is/content/files/public/postnrtrans.xslt" />
        -->
        <property name="casesStatuses" value="expXml:casesJbpmUtil.caseStatusesForHandling" />
        
    </properties>
</config>